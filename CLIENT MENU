<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Job Portal Menu</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 50px;
            background: #f4f9ff;
        }
        #main {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            border: 2px solid #0066cc;
            border-radius: 6px;
            background-color: #f4f9ff;
            box-shadow: 0 4px 8px rgba(0,102,204,0.2);
        }
        #lft {
            display: flex;
            align-items: center;
            gap: 20px;
            flex: 1;  /* allow search bar to grow */
        }
        #lft h2 {
            margin: 0;
            color: #0066cc;
            font-size: 22px;
            white-space: nowrap;
        }
        #search-bar {
            flex: 1; /* take available space */
        }
        #search-bar form {
            display: flex;
            width: 100%;
        }
        #search-bar input[type="text"] {
            flex: 1;
            padding: 8px 12px;
            border: 1px solid #0066cc;
            border-radius: 4px 0 0 4px;
            outline: none;
            font-size: 15px;
        }
        #search-bar button {
            padding: 8px 16px;
            background: #0066cc;
            color: white;
            border: none;
            border-radius: 0 4px 4px 0;
            cursor: pointer;
            transition: background 0.3s ease;
            font-size: 15px;
        }
        #search-bar button:hover {
            background: #003366;
        }
        #rgt {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-left: 30px;
        }
        #rgt a {
            text-decoration: none;
            color: #0066cc;
            font-weight: bold;
            font-size: 15px;
            transition: all 0.3s ease;
        }
        #rgt a:hover {
            color: #003366;
            background: #cce5ff;
            padding: 6px 10px;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <div id="main">
        <!-- Left: Job Portal + Wide Search bar -->
        <div id="lft">
            <h2>JOB PORTAL</h2>
            <div id="search-bar">
                <form action="searchJob.jsp" method="get">
                    <input type="text" name="query" placeholder="Search jobs, roles, companies...">
                    <button type="submit">Search</button>
                </form>
            </div>
        </div>

        <!-- Right: Menu links -->
        <div id="rgt">
            <a href="jobapply.jsp">JOB APPLY</a>
            <a href="searchJob.jsp">SEARCH JOB</a>
            <a href="jobStatus.jsp">JOB STATUS</a>
            <a href="profile.jsp">PROFILE</a>
        </div>
    </div>
</body>
</html>
